#!/bin/sh
OS=`uname -s`
makefile="mk/$OS.mk"
if [ ! -e "$makefile" ];then
	echo "** no Makefile for operating system $OS"
	exit 1
fi
which -s make
if [ "$?" -ne 0 ];then
	echo "** no make installed"
fi
cp $makefile Makefile
